name: Distributable

on:
  pull_request:
  push:
    branches: [main]

jobs:
  in-sync:
    name: upload/ matches the source tree
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: php tools/sync-distributable.php --check
